events {}

http {
  server {
    listen 80;


    location /api/ {
      proxy_pass http://api:8080/;
    }

    location /stream/ {
      types {
        application/vnd.apple.mpegurl m3u8;
        video/mp2t ts;
      }
      root /media/library;
    }
  }
}
